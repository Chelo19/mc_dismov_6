import 'package:flutter/material.dart';

class RedSocialPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Red Social'),
      ),
      body: Center(
        child: Text('Contenido de la Red Social'),
      ),
    );
  }
}
